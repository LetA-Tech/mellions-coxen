# Binaries
/bin/

# Secrets — these patterns match at any depth, not just the repository root.
.env
.env.*
!.env*.example
*.pem
*.key

# Logs and OS noise
*.log
.DS_Store

# Local ledger
*.db
*.db-wal
*.db-shm
dist/
config/ENVIRONMENT.md

# Local worktrees
.worktrees/

# Private release preparation and historical records belong only to the
# access-controlled source repository, never to a public candidate tree.
/internal-docs/
