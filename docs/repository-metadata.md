# Repository discoverability metadata

This file is the durable source for the GitHub repository settings that are configured through the GitHub UI/API rather than the code tree.

## About description

> **Engineering discipline and reliability for frontier coding agents: durable responsibility, adversarial verification, continuity, and autonomous completion.**

## Website

`https://letatech.ca/mellions-coxen`

## Topics

Use the focused set below. These are intended to describe capabilities Mellions genuinely supports or the engineering category it belongs to, not to maximize keyword count.

- `coding-agents`
- `agentic-engineering`
- `agent-reliability`
- `agent-evaluation`
- `adversarial-testing`
- `autonomous-coding`
- `long-running-agents`
- `agent-ops`
- `multi-agent`
- `ai-engineering`
- `software-engineering`
- `developer-tools`
- `human-agent-collaboration`
- `claude-code`
- `codex`

Intentionally omitted for now:

- `mcp` — Mellions inherits MCP from supported runtimes but is not itself an MCP integration product;
- `agentic-workflows` — Mellions explicitly avoids becoming a workflow engine;
- `agent-observability` — reporting and awareness exist, but observability is not the core product category;
- `loop-engineering` / `agent-loops` — useful positioning language to explore, but not needed as repository topics until the public community demonstrates that terminology is actually how users find the project.

## Social preview

Use a simple branded preview rather than a screenshot of terminal output.

Recommended content:

```text
Mellions Engineer
Turn frontier coding agents into engineers who stay on the job.

Engineering responsibility · falsification · continuity · autonomous completion
```

Use the LetA / Mellions visual identity already present in `assets/`. Do not place benchmark percentages in the social preview; the preview should remain useful even as evidence accumulates.

## Positioning vocabulary

Preferred:

- engineering responsibility layer for frontier coding agents;
- engineering discipline and reliability for frontier coding agents;
- reliability for long-running agent work;
- falsifiable autonomous engineering;
- AgentOps for serious software engineering, when explained rather than used as an unexplained category label.

Avoid presenting Mellions as:

- a general-purpose agent framework;
- an agent swarm platform;
- a replacement model runtime;
- a Claude-only tool;
- a workflow or approval engine.

## Model/runtime statement

Use this public distinction consistently:

> **Bring your frontier coding agent. Mellions Engineer adds the engineering discipline around it.**

Implemented runtimes today are Claude Code and Codex. Model compatibility can be inherited through those runtimes. Other runtimes require a verified adapter before they are described as supported.
